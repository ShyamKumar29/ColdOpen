/**
 * React <-> engine adapter hooks (docs/ARCHITECTURE.md section 5). Thin
 * bridges only — no business logic lives here (CLAUDE.md Project
 * Structure Rules).
 */
export * from './useCastRoster'
